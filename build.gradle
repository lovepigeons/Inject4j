// build.gradle (Groovy DSL)
plugins {
    id 'java'
}

group = 'org.oldskooler.inject4j'
version = '1.0.0'

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(8))
    }
}

java {
    withSourcesJar()
    withJavadocJar()
}

repositories {
    mavenCentral()
}

dependencies {
    // your deps
}
